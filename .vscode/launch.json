{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "gdb",
            "request": "attach",
            "name": "Attach to gdbserver",
            "executable": "${workspaceFolder}/build/iot_firmware.elf",
            "target": "localhost:4242",
            "remote": true,
            "cwd": "${workspaceRoot}",
            "gdbpath": "/usr/bin/avr-gdb",
            "preLaunchTask": "Start GDB Server"
        }
    ]
}